package org.machikoro.server.domain.enums

enum class EstablishmentType {
    AGRICULTURE,
    LIVESTOCK,
    RESOURCE,
    RETAIL,
    INDUSTRY,
    HOSPITALITY,
    MAJOR,
    // for later expansion, tbd if necessary
    MARITIME,
    ENTERPRISE,
    // not usable in current DB-setup, tbd if necessary - remove or change setup
    LANDMARK
}